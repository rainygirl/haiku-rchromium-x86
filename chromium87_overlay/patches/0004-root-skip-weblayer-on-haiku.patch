diff --git a/chromium/BUILD.gn b/chromium/BUILD.gn
--- a/chromium/BUILD.gn
+++ b/chromium/BUILD.gn
@@ -209,2 +209,3 @@ group("gn_all") {
-  if ((is_win || (is_linux && !is_chromeos) || is_android) && !use_qt) {
+  if (!is_haiku &&
+      (is_win || (is_linux && !is_chromeos) || is_android) && !use_qt) {
     deps += [
