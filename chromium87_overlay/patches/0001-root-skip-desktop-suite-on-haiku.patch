diff --git a/chromium/BUILD.gn b/chromium/BUILD.gn
--- a/chromium/BUILD.gn
+++ b/chromium/BUILD.gn
@@ -105,4 +105,5 @@ group("gn_all") {
-  if (!is_ios && !is_android && !is_chromecast && !is_fuchsia && !use_qt) {
+  if (!is_ios && !is_android && !is_chromecast && !is_fuchsia && !is_haiku &&
+      !use_qt) {
     deps += [
       "//chrome",
       "//chrome/browser/ui/color:dump_colors",
