--- a/chromium/v8/BUILD.gn
+++ b/chromium/v8/BUILD.gn
@@ -1756,6 +1756,13 @@
 }
 
 v8_source_set("v8_initializers") {
+  # Same 32-bit jumbo limit as torque_generated_initializers: this target's
+  # merged units are 1.3 MB, well past the 925 KB that is the largest one
+  # observed to compile here. Measured, not guessed -- gl_jumbo_1 at 911 KB and
+  # cc_jumbo_4 at 925 KB both build, and everything above that in this tree is
+  # V8.
+  never_build_jumbo = is_haiku
+
   visibility = [
     ":*",
     "test/cctest:*",
@@ -2238,6 +2245,13 @@
 
 # The src/compiler files with default optimization behavior.
 v8_source_set("v8_compiler") {
+  # Same 32-bit jumbo limit as torque_generated_initializers: this target's
+  # merged units are 1.0, 1.5 and 2.0 MB, well past the 925 KB that is the largest one
+  # observed to compile here. Measured, not guessed -- gl_jumbo_1 at 911 KB and
+  # cc_jumbo_4 at 925 KB both build, and everything above that in this tree is
+  # V8.
+  never_build_jumbo = is_haiku
+
   visibility = [ ":*" ]  # Only targets in this file can depend on this.
 
   sources = v8_compiler_sources
